spring:
  application:
    name: Avocatech
  datasource:
    url: jdbc:mysql://localhost:3306/avocatech
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MariaDBDialect